import AvatarIcon from "@/components/DataDisplay/Avatar";

const Notifications = () => {
  return (
    <AvatarIcon avatar="notifications" tooltip="Thông báo" type="icon-button" />
  );
};

export default Notifications;
